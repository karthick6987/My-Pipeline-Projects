🔹 Project 1: AKS + ACR Pipeline

Build Docker image

Push image to Azure Container Registry (ACR)

Deploy to AKS using GitHub Actions

Uses reusable workflows

uses security checks flow

Supports multi-environment (Dev / Prod)

🔹 Project 2: AKS + Docker image Pipeline

Build Docker image

Push image to Docker hub Registry

Deploy to AKS using GitHub Actions

Uses reusable workflows

Supports Multi-environment with deployment one after another(It waits for build AND staging)
